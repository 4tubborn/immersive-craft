[project]
name = "immersive-craft"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "4tubborn", email = "highocean106@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "beet>=0.118.0",
]

[project.scripts]
immersive-craft = "immersive_craft:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
